spring.application.name=redislab
# ========== Server ==========
server.port=8080

# ========== PostgreSQL ==========
spring.datasource.url=jdbc:postgresql://localhost:5432/redis_lab
spring.datasource.username=postgres
spring.datasource.password=postgres

#Specify PostgreSQL driver (Spring can usually auto-detect)
spring.datasource.driver-class-name=org.postgresql.Driver

# ========== JPA / Hibernate ==========
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ========== Redis ==========
spring.redis.host=localhost
spring.redis.port=6379
spring.cache.type=redis

#Redis timeout settings
spring.redis.timeout=60000
spring.redis.connect-timeout=60000

spring.main.allow-circular-references=true